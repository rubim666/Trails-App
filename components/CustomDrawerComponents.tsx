import { DrawerContentComponentsProps, DrawerContentScrollView } from '@react-navigation/drawer';
import { View, Text } from 'react-native';

const ITEMS = [
    { label: 'Perfil', screen: 'perfil' },
    { label: 'Conquistas', screen: 'conquistas' },
    { label: 'Histórico', screen: 'historico' },
    { label: 'Sair', screen: 'sair' },
]

export function CustomDrawerContent(props: DrawerContentComponentsProps) {
    return (
        // lista de itens do drawer
    )
}